# No external dependencies required - uses only the Python standard library
# (csv, json, dataclasses, pathlib). Kept here for consistency with other
# projects and in case future extensions add dependencies (e.g. requests
# for a live Management API integration).
